# Design: 本地优先的 AI 对话知识论坛 (Code name: Echo)

Generated by /office-hours on 2026-05-18
Branch: unknown
Repo: myNote
Status: APPROVED
Mode: Builder
Supersedes: (first design on this topic)

## Problem Statement

与 AI 的精妙对话在压缩、摘要、embedding 过程中丢失了最有价值的细节。市面上所谓"知识库"要么是云服务不可控（Notion 有容量上限），要么对 AI 不友好（Obsidian 的图谱看多了头皮发麻），要么跟 AI 完全两个世界（静态博客框架）。

核心痛点：**压缩即丢失**。好记性不如烂笔头——与其让算法猜你想要什么，不如原封不动记录下来，需要时精准找到。

## What Makes This Cool

1. **思维进化链**：每次 AI 检索旧对话生成新洞察，这个过程本身被记录下来，形成可追溯的思考脉络。旧点子 → 检索 → AI 合成 → 新点子 → 再记录。每一次思考都站在上一次的肩膀上。进化链条以"评论区"形式展示在每篇文章下方。

2. **零 token 导入**：剪贴板一键保存对话到 MD 文件，不经过 AI 处理，不消耗 token。这个功能需要使用ClaudeCode中的hook来实现。

3. **AI 搜索透明化**：不是黑盒返回一段总结——页面上明确显示"AI 读了这三篇文章，结论基于它们"。

4. **论坛隐喻**：对话当作帖子发表，有标签、摘要、时间线。帖子本身可在网页端编辑并同步回 MD 文件。

5. **文内标注式评论（核心交互）**：选中文章内任意一段文字 → 弹出评论框 → 发表评论。被评论的文字自动高亮（背景色变化），评论带有指向原文位置的锚点链接。这是思维进化链的 UI 形态——进化的不是整篇文章，而是文章里的**某个具体观点**。

6. **网页端编辑**：帖子正文、标签、摘要均可在浏览器中直接编辑，修改后写回 MD 文件的 frontmatter/正文。

## Constraints

- 本地优先，所有数据为 MD 文件 + YAML frontmatter
- GitHub 同步，零费用
- 基于 VitePress（不重复造渲染轮子）
- Node.js 技术栈（用户是前端程序员）
- MCP 接口供 AI 读写
- 不引入数据库、不引入搜索引擎（Elasticsearch 等重型依赖）
- 网页端编辑 → MD 文件同步必须是双向的

## Premises

1. MD + YAML frontmatter 作数据层，不引入数据库 —— **已确认**
2. 全文检索 + 标签 + 来源可追溯 >> 黑盒语义搜索。核心是思维进化链 —— **已确认**
3. VitePress 做展示骨架，不自研渲染引擎 —— **已确认**
4. 本地优先 + GitHub 同步，零费用 —— **已确认**

## Approaches Considered

### Approach A: 极简版 (2-3天)
VitePress + vitepress-plugin-mcp + 剪贴板脚本。MD 放 docs/ 目录，VitePress 自动渲染。MCP 插件提供基础搜索。标签放 frontmatter。摘要手动维护。
- Effort: S
- Risk: Low
- Pros: 最快上线，零依赖开发
- Cons: 缺少标签管理 UI、搜索结果来源展示、思维进化链、网页端编辑功能
- Reuses: VitePress, vitepress-plugin-mcp

### Approach B: 完整版 (1-2周) ← RECOMMENDED
VitePress + 自研 MCP Server + 标签管理页面 + 剪贴板自动化 + 网页端编辑同步。
- Effort: M
- Risk: Low
- Pros: 标签管理、搜索结果可追溯、思维进化链、MCP 工具精细控制、网页端编辑写回 MD
- Cons: 需要写 MCP server、VitePress theme 扩展、前端编辑组件
- Reuses: VitePress 渲染层、VitePress 内置搜索、MCP TypeScript SDK

### Approach C: 论坛版 (2-3周)
Next.js 自建论坛界面 + MCP + 完整定制。
- Effort: L
- Risk: Med
- Pros: 最接近"本地论坛"愿景，交互自由度最大
- Cons: 开发量大，维护成本高，Next.js 对 MD 渲染不如 VitePress 开箱即用
- Reuses: Next.js, MCP SDK

## Recommended Approach

**Approach B — VitePress + 自研 MCP Server**

理由：在 VitePress 生态内最大化独特需求。vitepress-plugin-mcp 证明了 VitePress + MCP 是可行的，但它只有 `search_vitepress_docs` 一个工具。我们需要在此基础上扩展：更精细的搜索（按标签、按时间范围）、搜索结果带来源展示、标签管理、思维进化链的读写。

## Architecture Overview

```
~/echo/                          ← 项目根目录
├── docs/                        ← VitePress 内容目录（即知识库）
│   ├── index.md                 ← 首页（最新对话列表）
│   ├── 2026-05-18-xxx.md        ← 单篇对话（文件名 = 日期 + slug）
│   ├── 2026-05-18-xxx/          ← 思维进化链子目录（同级文件夹）
│   │   └── evolution-01.md      ← 进化链条目
│   └── .vitepress/
│       └── config.mts           ← VitePress 配置
├── mcp-server/                  ← 自研 MCP Server
│   └── src/
│       ├── index.ts             ← MCP server 入口 (stdio)
│       └── tools.ts             ← MCP 工具定义
├── scripts/
│   └── paste-to-md.sh           ← 剪贴板 → MD 脚本 (macOS)
├── server/                      ← 本地 API server（网页端编辑需要）
│   └── src/
│       ├── index.ts             ← Express / 纯 Node HTTP server
│       └── file-api.ts          ← MD 文件读写 API
├── .github/
│   └── workflows/
│       └── deploy.yml           ← GitHub Pages / 自动同步
└── package.json
```

### 数据格式

每篇对话 MD 文件的 frontmatter：

```yaml
---
title: "与 Claude 讨论知识库设计"
date: 2026-05-18
tags: [知识库, MCP, 前端]
summary: "讨论了本地优先的知识论坛方案，确定了 VitePress + MCP 的技术路线"
ai_model: claude-opus-4-7
evolution_of: null          # 如果是进化版，指向原始文章的 slug
---
```

### 网页端编辑 API

本地 HTTP server 提供文件写入能力（VitePress 只读渲染，不能写）：

| 端点 | 功能 |
|------|------|
| `POST /api/save` | 保存/更新 MD 文件（正文 + frontmatter） |
| `POST /api/annotate` | 对文章中某段文字添加评论（标注锚点、评论内容、评论者） |
| `GET /api/annotations?file=xxx` | 获取某篇文章的所有文内标注 |
| `PUT /api/tags` | 更新文章标签 |
| `PUT /api/summary` | 更新文章摘要 |

网页编辑流程：用户在浏览器编辑 → POST 到本地 server → server 写 MD 文件 → VitePress HMR 自动刷新显示。

### MCP 工具列表

| 工具名 | 功能 |
|--------|------|
| `search_articles` | 全文 + 标签搜索，返回匹配文章列表（含路径、标题、标签、摘要） |
| `get_article` | 读取单篇文章全文 |
| `get_article_context` | 读取文章 + 其标注评论 + 关联文章 |
| `list_tags` | 列出所有标签及使用次数 |
| `list_recent` | 最近 N 篇文章 |
| `get_annotations` | 获取某篇文章的所有文内标注（选中文字 + 评论内容 + 锚点位置） |
| `add_annotation` | 对文章中某段文字添加评论标注 |

### 关键交互

1. **剪贴板保存**：`scripts/paste-to-md.sh` 读取剪贴板 → 生成带 frontmatter 的 MD 文件 → 放到 docs/ 目录
2. **AI 搜索 + 来源展示**：用户通过 MCP client 提问 → MCP server 搜索 docs/ 中的 MD → 返回匹配文章 + 在页面上显示"AI 读了 X、Y、Z 三篇文章"
3. **文内标注评论**：选中文章内任意文字 → 弹框写评论 → 发表后该文字高亮 + 评论锚点链接。评论数据存储在原文同级目录的 `annotations.json` 或通过特殊的 Markdown 标注语法存储
4. **思维进化链**：被评论的文字片段和评论本身组成进化链。当 AI 对某个观点产生新想法，它以"评论"形式附加到原文具体段落上
5. **网页端编辑**：浏览器中点击"编辑"按钮 → 修改标题/标签/摘要/正文 → 提交 → 本地 API server 写回 MD 文件 → 页面即时更新

## Future Versions

### v2: 上下文池 (Context Pool)

从不同文章中选取多段文字放入一个共享的"上下文池"，AI 基于池内所有片段进行跨文章讨论与合成。

- **选择 → 入池**：在任何文章中选中一段文字，点击"加入上下文池"
- **池管理**：侧边栏或浮窗展示当前池中的所有片段（来源文章、原文、加入时间），支持删除和排序
- **AI 讨论**：将池中所有片段作为上下文发给 AI，AI 基于跨文章内容进行讨论、对比、合成
- **产出回写**：讨论结果可保存为新文章，或作为标注挂载到任意原文段落

与文内标注的关系：
- 文内标注 = 纵向深度（在原文上"钉"评论）
- 上下文池 = 横向拼接（跨文章"捞"片段放到一起讨论）
- 两者组合形成完整思维进化链

## Open Questions

- 剪贴板脚本如何跨平台（Windows/Linux 用户）？
- 标签的 AI 自动建议：是让 AI 一次性处理所有文章打标签，还是每次导入时处理？
- 多设备同步：纯 Git 还是需要可选的其他方案？
- 搜索是否需要前端搜索界面（在浏览器里直接搜），还是纯靠 MCP？

## Success Criteria

- [ ] `paste-to-md.sh` 能一键将剪贴板内容保存为带 frontmatter 的 MD
- [ ] `npm run dev` 启动 VitePress 后浏览器能看到所有对话列表
- [ ] MCP server 的 `search_articles` 能根据关键词/标签返回匹配文章
- [ ] AI 通过 MCP 搜索后，页面上能看到"AI 读了哪些文章"
- [ ] 思维进化链（evolution_of）能正确展示为"评论区"
- [ ] 网页端编辑标签/摘要/正文能同步写回 MD 文件
- [ ] GitHub 上其他人 clone 后能 `npm install && npm run dev` 跑起来

## Distribution Plan

- 代码托管在 GitHub 公开仓库
- 用户 clone 后本地运行：`npm install && npm run dev`
- MCP server 通过 mcp.json 中配置 stdio 连接
- 未来可考虑 npm 发包：`npx echo-mcp` 一行启动 MCP server

## Next Steps

1. 搭建 VitePress 最小项目，确认 MD 渲染和目录结构
2. 设计 MD 文件的 frontmatter 规范（标签、摘要、进化链字段）
3. 实现 `paste-to-md.sh`（macOS 优先）
4. 实现 MCP server 骨架（`search_articles` 和 `get_article`）
5. 写一篇示例对话导入并验证端到端流程
6. 实现本地 API server（网页端编辑 → MD 写入）
7. 实现标签管理页面和网页端编辑组件
8. 实现搜索结果来源展示
9. 实现思维进化链（评论区）展示

## What I noticed about how you think

- "好记性不如烂笔头"是你贯穿始终的哲学——你不信算法比你自己更懂你的想法，你信的是详实的记录和精准的检索。这是一种**高信任度知识管理**立场，跟主流"智能推荐"路线完全相反。
- 你提出的"思维进化链"概念——旧点子被检索后成为 AI 上下文、生成新点子、链条可追溯——这是你在讨论过程中自发提炼出来的，不是我从你那里提取的。这说明你对知识的看法是**动态的、进化的**，不是静态归档。
- 你对费用极其敏感（"大家都穷"），但你敏感的不是钱本身，而是**数据和访问权不应该被付费墙控制**。这让你自然走向了开源 + 本地优先的路线。
- 你选了 B 方案而不是 A——宁愿多花一周把 MCP 做精细，也不满足于"能跑就行"。这说明你对工具质量有要求，不是敷衍了事的人。
